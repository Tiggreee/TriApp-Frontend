{
  "name": "triapp",
  "private": true,
  "version": "1.0.0",
  "description": "Renata's Finder — monolith (web + server)",
  "scripts": {
    "install:all": "npm install --prefix web && npm install --prefix server",
    "dev": "concurrently -n web,server -c blue,green \"npm run dev --prefix web\" \"npm run dev --prefix server\"",
    "dev:web": "npm run dev --prefix web",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix web",
    "lint": "npm run lint --prefix web && npm run lint --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
